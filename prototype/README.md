# Org Pulse Prototype

This is a frontend-only prototype with hardcoded role-hierarchy fixture data. It does not connect to Jira, Linear, GitHub, Slack, Teams, or any backend.

Open `index.html` in a browser to review the static prototype.
